# Review this example setting before deployment.
# Review this example setting before deployment.

# Review this example setting before deployment.
# Review this example setting before deployment.
AUTH_ENABLED=true

# Replace with a generic non-personal value if needed.
ADMIN_USERNAME=example-user

# Provide a deployment-specific value before use.
# Provide a deployment-specific value before use.
# Provide a deployment-specific value before use.
ADMIN_PASSWORD_HASH=

# Review this example setting before deployment.
# Review this example setting before deployment.
# Adjust this example host for your environment if needed.
ALLOWED_ALIST_HOSTS=

# Review this example setting before deployment.
# Review this example setting before deployment.
ALIST_ENV_OVERRIDE_ENABLED=false

# Adjust this example endpoint for your environment if needed.
# Review this example setting before deployment.
TRANSCODE_BACKENDS=qsv,vaapi,cpu

# Adjust this example endpoint for your environment if needed.
# Adjust this example endpoint for your environment if needed.
# Adjust this example endpoint for your environment if needed.
# Adjust this example endpoint for your environment if needed.
# Adjust this example endpoint for your environment if needed.
# Adjust this example endpoint for your environment if needed.
# Adjust this example endpoint for your environment if needed.
# Adjust this example endpoint for your environment if needed.

# Adjust this example endpoint for your environment if needed.
ALIST_TGDOWNLOADER_URL=https://example.invalid
ALIST_TGDOWNLOADER_USERNAME=example-user
ALIST_TGDOWNLOADER_PASSWORD=

# Adjust this example endpoint for your environment if needed.
ALIST_DOWNLOADS_URL=https://example.invalid
ALIST_DOWNLOADS_USERNAME=example-user
ALIST_DOWNLOADS_PASSWORD=

# Adjust this example endpoint for your environment if needed.
ALIST_SOURCE_3_URL=https://example.invalid
ALIST_SOURCE_3_USERNAME=example-user
ALIST_SOURCE_3_PASSWORD=
